/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/martiinii-portfolio-v2',
    // mdxRs is rust-based MDX compiler. Not recommended for production
    // experimental: {
    //     mdxRs: true
    // }
}

export default nextConfig